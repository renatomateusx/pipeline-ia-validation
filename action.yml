name: 'IA Direct OpenAI'
description: 'Valida payload diretamente com OpenAI, com gerenciamento automático de tokens'
inputs:
  openai_token:
    required: true
    description: "Token da API OpenAI"
  payload:
    required: true
    description: "JSON para validação"
runs:
  using: "node20"
  main: "dist/index.js"